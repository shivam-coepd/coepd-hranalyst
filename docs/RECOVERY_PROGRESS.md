# Recovery Progress

| Batch | Finding and root cause | Files or migrations | Tests and result | Remaining risk |
| --- | --- | --- | --- | --- |
| Repository safety and inventory | Main repository was inconsistent with staged references; work began on a dedicated recovery branch without reading secret values | Inventory JSON, route matrix, recovery records | File inventory reports no empty files | Original sibling stage folders later disappeared outside this work; recovered content is checkpointed here |
| Source recovery | 102 required candidates were absent or empty after interrupted staged work | Restored application, component, service, repository, API, migration, and SQL-test files | Type and route audits pass | Detailed source paths are unavailable after the external folder disappearance |
| Database reconstruction | Later migrations had ambiguous ordering, conflicting state constraints, unsafe API-role defaults, and deployed schema drift | Ordered migrations 1–19; claim RPC; tenant RLS; privilege hardening; schema alignment | All migrations apply from zero; all SQL files pass on full PostgreSQL | Linked target deployment remains pending |
| Workflow reconciliation | Application services and schema used divergent scoring and offer states | Scoring, verification, submission, mocks, interviews, offers, placements | 20 unit tests and stage SQL contracts pass | Provider-backed workflows require configured services |
| Security | Service-role repositories could bypass tenant rules without explicit filters | Guards, scoped repositories, final RLS policies, signed-file routes, API error handler | Tenant-isolation SQL probes and anonymous API denial pass | Authenticated browser role matrix needs seeded accounts |
| Notifications | Queue ownership and terminal retry behavior were incomplete | Migration 16 and notification worker/services | Delivery, retry, stale-claim, fenced-completion, and dead-letter SQL tests pass | Live channels need provider credentials |
| Quality and release | Duplicate helpers, suppressions, broken links, and incomplete test tooling obscured failures | Canonical proxy/clients, Vitest, Playwright, migration harness, release/performance scripts | Links, unit, database, type, lint, build, E2E, and production audit gates pass | Representative load measurement requires an external seeded target |
| Documentation | Recovery artifacts vanished with the untracked docs directory after an environment retry | All required docs, PRD extract, inventories, traceability and runbooks restored | Documentation names and route inventory verified against the current tree | Keep the Git checkpoint as the durable recovery record |
