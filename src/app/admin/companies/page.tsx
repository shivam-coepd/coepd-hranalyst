import Link from "next/link"; import { requireAdmin } from "@/lib/auth/guards"; import { getCompanies } from "@/repositories/companies.repository";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string;status?:string}>}){await requireAdmin();const p=await searchParams;const r=await getCompanies({search:p.q,status:p.status});return <main className="p-8"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-bold">Companies</h1><p className="text-slate-600">Verify and manage client organizations.</p></div><Link className="rounded bg-slate-950 px-4 py-2 text-white" href="/admin/companies/new">Add company</Link></div><form className="mt-6 flex gap-2"><input name="q" defaultValue={p.q} placeholder="Search" className="rounded border px-3 py-2"/><select name="status" defaultValue={p.status??""} className="rounded border px-3 py-2"><option value="">All statuses</option><option>pending</option><option>verified</option><option>rejected</option></select><button className="rounded border px-3 py-2">Filter</button></form><div className="mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th className="p-3">Company</th><th>Domain</th><th>Status</th><th>Active</th><th></th></tr></thead><tbody>{r.companies.map(c=><tr key={c.id} className="border-t"><td className="p-3 font-medium">{c.name}</td><td>{c.domain??"—"}</td><td>{c.verification_status}</td><td>{c.is_active?"Yes":"No"}</td><td><Link className="underline" href={`/admin/companies/${c.id}`}>Open</Link></td></tr>)}</tbody></table></div></main>}
