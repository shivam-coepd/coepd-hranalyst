import { NextResponse } from "next/server"; import { createCompany } from "@/services/companies/company.service"; import { getCompanies } from "@/repositories/companies.repository";
export async function GET(req:Request){try{const u=new URL(req.url);return NextResponse.json(await getCompanies({status:u.searchParams.get("status")||undefined,search:u.searchParams.get("q")||undefined}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Request failed"},{status:400})}}
export async function POST(req:Request){try{return NextResponse.json(await createCompany(await req.json()),{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Request failed"},{status:400})}}
