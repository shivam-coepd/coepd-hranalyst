import { NextResponse } from "next/server"; import { rejectCompany } from "@/services/companies/company.service";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const{reason}=await req.json();return NextResponse.json(await rejectCompany(id,String(reason??"")))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Request failed"},{status:400})}}
