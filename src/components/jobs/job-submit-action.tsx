"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export default function JobSubmitAction({jobId,disabled=false}:{jobId:string;disabled?:boolean}){const [busy,setBusy]=useState(false);const[m,setM]=useState("");const r=useRouter();async function submit(){if(!confirm("Submit this job for checklist review? Draft editing will stop."))return;setBusy(true);const res=await fetch(`/api/jobs/${jobId}/submit`,{method:"POST"});const j=await res.json().catch(()=>({}));setBusy(false);if(!res.ok){setM(j.error??"Unable to submit");return;}r.refresh();}return <div><button disabled={disabled||busy} onClick={submit} className="rounded bg-blue-700 px-3 py-2 text-white disabled:opacity-50">{busy?"Submitting...":"Submit for checklist"}</button>{m&&<p className="mt-2 text-sm text-red-700">{m}</p>}</div>}
